apiVersion: v1
kind: Service
metadata:
# same name as memcached service in app.properties file
  name: vprocache01
spec:
  selector:
    app: vpromc
  ports:
  - port: 11211
    targetPort: vpromc-port
    protocol: TCP
  type: ClusterIP