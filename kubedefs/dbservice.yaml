apiVersion: v1
kind: Service
metadata:
# this is in app.config file vprodb:3306/---
  name: vprodb
spec:
  selector:
    app: vprodb
  ports:
  - port: 3306
    targetPort: vprodb-port
    protocol: TCP
  type: ClusterIP